# Northwind RAG/MCP Merge Files

These are the selected files from the 2026-03-24 Northwind RAG/MCP project.

Copy them into your cloned repository: northwind-fullstack.

Do NOT copy .env files, node_modules, or certificates. They were intentionally excluded.

After copying, run:

```bash
git status
```

Then send the output before committing.
