import { createTheme } from "@mui/material";

export const appTheme = createTheme({
    typography: {
        fontFamily: "cursive"
    },
    palette: {
        primary: {
            main: "#871f1fff"
        },
        secondary: {
            main: "#cbc139ff"
        }
    }
});
